const auth = require("./auth");
const channels = require("./channels");
const videos = require("./videos");
const subscriptions = require("./subscriptions");

module.exports = { auth, channels, videos, subscriptions };
